# Проверка чистого клона B5

2026-09-23, 16:31–16:34 UTC+05:00. Проверен опубликованный `backend` (`3ecef28`).

- Новый клон GitHub, отдельный venv, установка `data/requirements.txt`.
- Python 3.13.7, ecCodes 2.48.0, Node 22.23.2, Windows.
- Backend: 66 тестов; Data: 41; UI: 26. Все прошли.
- `python -m backend.server` и `node ui/server.mjs`: успешный запуск.
- `node ui/smoke-api.mjs`: два настоящих HTTP/OpenAI-запроса, по 48 точек.
  - 31 января / 24 часа / две турбины: `resp_0fcd2fa794b337d4016ab3b8d1af6c87d2965b7b5da29a5c26`.
  - 14 февраля / 48 часов / турбина 2: `resp_0ae615f62b58f34f016ab3b8dac41c87d2afa31dcf1d98ead0`.
- UI `/`, `/api.mjs`, `/results.mjs`, `/privacy.html`: HTTP 200.
- Повтор `backend.batch_forecast`: 28 запусков, 2688 точек, CSV 1344 строки; SHA256 CSV совпадает с включённым отчётом.
- Рабочее дерево чистого клона осталось чистым. Ключ только в игнорируемом `.env`.

Проверка обнаружила лишний `-t .` в README-команде Data unittest discovery; документация исправлена
на штатный `python -m unittest discover -s data/tests -v`. Исходники Data и чистого клона не менялись.

Живая загрузка NOAA отдельно подтверждена в `live-cycle.json`: повторная загрузка 81.55 секунды,
затем реальный расчёт и OpenAI. Параллельный декодер ecCodes на Windows завершал процесс;
backend использует один worker, исправление подтверждено живым прогоном.

Docker Engine и доступный браузер отсутствуют. Контейнерная и визуальная проверки не выполнены.
Февральских фактических мощностей нет: эти результаты подтверждают воспроизводимость, не точность модели.
